import React from 'react';
import ReactDOM from 'react-dom/client';
import TermoReportApp from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TermoReportApp />
  </React.StrictMode>
);